export const geoPoint = (lat,lon) => `${lat},${lon}`;
export const geoPointString = (str) => str;
export const instant = (a) => a;
export const localDate = (a) => a;
export const localDateTime = (a) => a;
export const localTime = (a) => a;
