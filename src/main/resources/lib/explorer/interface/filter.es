export const filter = ({
	_id,
	_name,
	_path,
	displayName,
	filters,
	queryJson,
	resultMappings,
	type
} = {}) => ({
	_id,
	_name,
	_path,
	displayName,
	filters,
	queryJson,
	resultMappings,
	type
});
